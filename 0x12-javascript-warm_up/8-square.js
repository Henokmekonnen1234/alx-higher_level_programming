#!/usr/bin/node
const process = require('process');
const args = process.argv[2];
if (praseInt(args)) {
  for (let i = 1; i <= args; i++) {
    for (let i = 1; i <= args; i++) {
      console.log('X');
}}} else {
  console.log('Missing size');
}
